flask>=2.3.0
flask-cors>=4.0.0
numpy>=1.24.0
# 缩写识别模型依赖包
# 安装命令: pip install -r requirements.txt

# PyTorch (CUDA 12.4 版本 - 支持 PyTorch 2.6.0+)
# 注意: 如需GPU支持，请使用以下命令安装:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0


# Transformers 和 Tokenizers
transformers>=4.30.0,<5.0.0
tokenizers>=0.13.0

# 数据集处理
datasets>=2.12.0

# LoRA 微调
peft>=0.4.0

# 拼音处理
pypinyin>=0.49.0

# 中文分词
jieba>=0.42.1

# 评估指标
evaluate>=0.4.0

# 机器学习工具
scikit-learn>=1.2.0

# 数据处理
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# 进度条
tqdm>=4.65.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# TensorBoard (可选，用于训练可视化)
tensorboard>=2.13.0

# 其他工具
accelerate>=0.20.0
safetensors>=0.3.0
huggingface-hub>=0.16.0
