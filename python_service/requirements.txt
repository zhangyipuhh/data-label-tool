# ========================================
# 安装说明
# ========================================
# PyTorch GPU 版本需要从官方源安装（不支持镜像）：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# 其他依赖包可使用镜像源安装（选一即可）：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#   pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
# ========================================

flask>=2.3.0
flask-cors>=4.0.0
waitress>=2.1.0
numpy>=1.24.0

# PyTorch (CUDA 12.8 版本)
# 默认 pip install torch 安装的是 CPU 版本，不支持 GPU
# GPU 版本安装命令:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# 或使用 conda 安装:
# conda install pytorch torchvision torchaudio pytorch-cuda=12.8 -c pytorch -c nvidia
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# Transformers 和 Tokenizers
transformers>=4.30.0,<5.0.0
tokenizers>=0.13.0

# 数据集处理
datasets>=2.12.0

# LoRA 微调
peft>=0.4.0

# 拼音处理
pypinyin>=0.49.0

# 中文分词
jieba>=0.42.1

# 评估指标
evaluate>=0.4.0

# 机器学习工具
scikit-learn>=1.2.0

# 数据处理
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

# 进度条
tqdm>=4.65.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0

# TensorBoard (可选，用于训练可视化)
tensorboard>=2.13.0

# 其他工具
accelerate>=0.20.0
safetensors>=0.3.0
huggingface-hub>=0.16.0